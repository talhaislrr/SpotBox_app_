const { getDefaultConfig } = require("@expo/metro-config");

/**
 * Metro yapılandırması
 * Daha fazla bilgi: https://docs.expo.dev/workflow/metro/
 */
const config = getDefaultConfig(__dirname);

module.exports = config; 